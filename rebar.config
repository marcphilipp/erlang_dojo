{erl_opts, [
    warnings_as_errors
]}.

{profiles, [
    {test, [
        {deps, [
            {meck, "0.8.9"}
        ]}
    ]}
]}.

{eunit_opts, [verbose, {report, {eunit_surefire, [{dir, "_build/test"}]}}]}.
